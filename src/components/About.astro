---
import { Icon } from "astro-icon/components";

const valores = [
  {
    icon: "lucide:rocket",
    title: "Velocidad",
    description: "Entregamos resultados rápidos sin sacrificar calidad.",
    fill: "#67e8f9",
  },
  {
    icon: "lucide:shield-check",
    title: "Seguridad",
    description:
      "Tu información siempre protegida con los más altos estándares.",
    fill: "#a5b4fc",
  },
  {
    icon: "lucide:headphones",
    title: "Soporte",
    description: "Estamos contigo en cada paso, antes y después de la entrega.",
    fill: "#7dd3fc",
  },
  {
    icon: "lucide:settings",
    title: "Personalización",
    description:
      "Cada solución se adapta 100% a las necesidades de tu negocio.",
    fill: "#c4b5fd",
  },
];
---

<section
  class="theme-light relative min-h-[100dvh] overflow-hidden py-5 sm:py-14 md:py-20"
  id="nosotros"
  data-nav-theme="light"
  data-pin-section
>
  <div class="container mx-auto px-6 relative z-10">
    <div class="max-w-7xl mx-auto">
      <!-- Header -->
      <div class="mb-4 max-w-3xl sm:mb-10 md:mb-14">
        <p class="eyebrow mb-2 sm:mb-4 md:mb-6" data-reveal="fade">03 — Nosotros</p>
        <h2
          class="text-[clamp(2.5rem,5.5vw,4.5rem)] font-bold tracking-[-0.03em] leading-[1.05] text-slate-900"
          data-reveal="lines"
        >
          ¿Quiénes <span class="text-cyan-700">somos</span>?
        </h2>
        <p
          class="mt-2 max-w-2xl text-sm leading-relaxed text-slate-600 sm:mt-4 sm:text-base md:mt-6 md:text-lg"
          data-reveal="fade-up"
          data-reveal-delay="0.15"
        >
          Más de dos años construyendo software para negocios reales. Hablamos
          tu idioma y nos enfocamos en una sola cosa: que cada inversión
          digital se traduzca en resultados concretos.
        </p>
      </div>

      <!-- Values Grid -->
      <div
        class="grid grid-cols-1 gap-2 sm:grid-cols-2 sm:gap-4 lg:grid-cols-4 lg:gap-6"
        data-reveal="stagger"
        data-reveal-stagger="0.1"
      >
        {
          valores.map((valor) => (
            <div
              class="value-card group relative isolate min-h-[5.75rem] overflow-hidden rounded-xl border border-slate-200/80 bg-white/75 p-3 shadow-[0_0.5rem_1.5rem_-1.25rem_rgb(15_23_42_/_0.35)] backdrop-blur-sm sm:h-[22rem] sm:rounded-2xl sm:p-6 lg:h-[25rem] lg:p-8"
              style={`--card-fill: ${valor.fill}`}
            >
              <span class="value-card__fill" aria-hidden="true"></span>
              <div class="value-card__content relative z-10 flex h-full items-center gap-4 sm:flex-col sm:items-stretch sm:gap-0">
              <div class="flex h-11 w-11 shrink-0 items-center justify-center rounded-lg border border-cyan-100 bg-cyan-50/80 shadow-sm transition-all duration-700 group-hover:shadow-sm sm:h-14 sm:w-14 sm:rounded-xl sm:bg-white lg:h-16 lg:w-16">
                <Icon
                  name={valor.icon}
                  class="text-cyan-700 transition-colors duration-500 group-hover:text-slate-900"
                  size={28}
                />
              </div>
              <div class="min-w-0 sm:contents">
                <h3 class="font-display text-base font-medium leading-tight text-slate-950 sm:mt-16 sm:text-xl lg:mt-20 lg:text-2xl">
                  {valor.title}
                </h3>
                <p class="mt-1 text-xs leading-snug text-slate-600 sm:mt-auto sm:pt-8 sm:text-sm sm:leading-relaxed sm:text-slate-700">
                  {valor.description}
                </p>
              </div>
              </div>
            </div>
          ))
        }
      </div>

      <!-- Stats Section -->
      <!-- <div
        class="mt-16 p-8 md:p-12 rounded-2xl bg-gradient-to-r from-slate-900/60 to-slate-900/40 border border-white/5"
      >
        <div class="grid grid-cols-2 md:grid-cols-4 gap-8 text-center">
          <div>
            <div
              class="text-4xl md:text-5xl font-display font-bold text-white mb-2"
            >
              50+
            </div>
            <p class="text-slate-400 text-sm uppercase tracking-wider">
              Proyectos entregados
            </p>
          </div>
          <div>
            <div
              class="text-4xl md:text-5xl font-display font-bold text-white mb-2"
            >
              98%
            </div>
            <p class="text-slate-400 text-sm uppercase tracking-wider">
              Clientes satisfechos
            </p>
          </div>
          <div>
            <div
              class="text-4xl md:text-5xl font-display font-bold text-white mb-2"
            >
              48h
            </div>
            <p class="text-slate-400 text-sm uppercase tracking-wider">
              Tiempo de respuesta
            </p>
          </div>
          <div>
            <div
              class="text-4xl md:text-5xl font-display font-bold text-white mb-2"
            >
              24/7
            </div>
            <p class="text-slate-400 text-sm uppercase tracking-wider">
              Soporte disponible
            </p>
          </div>
        </div>
      </div> -->
    </div>
  </div>
</section>

<style>
  #nosotros {
    /* Gradientes estáticos: conservan la atmósfera sin rasterizar dos blurs
       gigantes en cada frame mientras la sección está pinneada. */
    background-image:
      radial-gradient(circle at 105% 18%, rgb(207 250 254 / 0.7), transparent 34rem),
      radial-gradient(circle at -8% 88%, rgb(224 231 255 / 0.65), transparent 32rem);
  }

  .value-card {
    transition:
      border-color 800ms cubic-bezier(0.22, 1, 0.36, 1),
      box-shadow 800ms cubic-bezier(0.22, 1, 0.36, 1);
  }

  .value-card__fill {
    position: absolute;
    inset: 0;
    z-index: 0;
    background: var(--card-fill);
    transform: translateY(100%);
    transition: transform 900ms cubic-bezier(0.22, 1, 0.36, 1);
  }

  .value-card__content,
  .value-card h3,
  .value-card p {
    transition: color 700ms cubic-bezier(0.22, 1, 0.36, 1);
  }

  @media (hover: hover) and (pointer: fine) {
    .value-card:hover {
      border-color: transparent;
      box-shadow: 0 1.5rem 3.5rem -2rem color-mix(in srgb, var(--card-fill) 55%, transparent);
    }

    .value-card:hover .value-card__fill {
      transform: translateY(0);
    }

    .value-card:hover h3 {
      color: rgb(15 23 42);
    }

    .value-card:hover p {
      color: rgb(30 41 59);
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .value-card,
    .value-card__fill {
      transition-duration: 0.01ms;
    }
  }
</style>
