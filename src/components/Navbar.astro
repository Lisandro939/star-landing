---
import { Icon } from "astro-icon/components";
import { Image } from "astro:assets";

const menuItems = [
  { label: "Inicio", href: "#inicio" },
  { label: "Soluciones", href: "#soluciones" },
  { label: "Proyectos", href: "#servicios" },
  { label: "Planes", href: "#planes" },
  { label: "Nosotros", href: "#nosotros" },
  { label: "Empezá hoy", href: "#cta" },
];
---

<nav
  id="site-nav"
  class="fixed inset-x-0 top-0 z-50"
  aria-label="Navegación principal"
>
  <div class="mx-auto w-full max-w-[120rem] px-5 sm:px-7">
    <div
      class="nav-pill flex h-20 items-center justify-between border-b md:h-[5.5rem]"
    >
      <!-- LOGO: dos copias superpuestas, el swap lo hace CSS via [data-nav-theme] -->
      <a href="/" class="relative flex items-center" aria-label="Yzen Software — Inicio">
        <Image
          src="/assets/Yzen-Logo-With-Name-Color.png"
          alt="Yzen Software"
          width={96}
          height={32}
          class="nav-logo-color w-[6.5rem] md:w-[7rem]"
        />
        <Image
          src="/assets/Yzen-Logo-With-Name-White.png"
          alt=""
          aria-hidden="true"
          width={96}
          height={32}
          class="nav-logo-white absolute top-0 left-0 w-[6.5rem] md:w-[7rem]"
        />
      </a>

      <!-- DESKTOP MENU -->
      <div class="hidden items-center gap-5 lg:gap-7 xl:gap-8 md:flex">
        {
          menuItems.map((item) => (
            <a
              href={item.href}
              class="nav-link group/link relative py-3 text-[0.72rem] font-semibold tracking-[-0.01em] lg:text-[0.78rem]"
            >
              {item.label}
              <span class="absolute bottom-1 left-0 h-px w-0 bg-current transition-all duration-300 group-hover/link:w-full" />
            </a>
          ))
        }

        <a href="#contacto" class="nav-contact btn-cta btn-cta--solid text-xs lg:text-sm">
          Contacto
          <span class="btn-arrows" aria-hidden="true">
            <svg viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M3 8h10M9 4l4 4-4 4"></path></svg>
            <svg viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M3 8h10M9 4l4 4-4 4"></path></svg>
          </span>
        </a>
      </div>

      <!-- MOBILE MENU BUTTON -->
      <div class="flex items-center gap-2 md:hidden">
        <a href="#contacto" class="mobile-nav-cta btn-cta btn-cta--solid">
          Contacto
          <span aria-hidden="true">→</span>
        </a>
        <button
          class="nav-link mobile-nav-menu relative z-[60] flex items-center justify-center"
        id="menuToggle"
        aria-label="Abrir menú"
        aria-expanded="false"
        aria-controls="mobileMenuPanel"
      >
        <span class="menu-icon-open">
          <Icon name="lucide:menu" size={21} />
        </span>
        <span class="menu-icon-close hidden">
          <Icon name="lucide:x" size={21} />
        </span>
        </button>
      </div>
    </div>
  </div>
</nav>

<!-- MOBILE MENU OVERLAY -->
<div
  id="mobileMenuOverlay"
  class="pointer-events-none fixed inset-0 z-[55] bg-slate-950/80 opacity-0 backdrop-blur-sm transition-opacity duration-300 md:hidden"
>
</div>

<!-- MOBILE MENU PANEL -->
<div
  id="mobileMenuPanel"
  class="fixed top-0 right-0 z-[56] h-full w-[280px] max-w-[85vw] translate-x-full border-l border-white/10 bg-slate-950/95 backdrop-blur-xl transition-transform duration-300 ease-out md:hidden"
>
  <div class="flex h-full flex-col px-6 pt-20 pb-8">
    <!-- MOBILE MENU LINKS -->
    <nav class="flex flex-col gap-2" aria-label="Menú mobile">
      {
        menuItems.map((item, index) => (
          <a
            href={item.href}
            class="mobile-menu-link group relative rounded-xl px-4 py-4 text-lg font-semibold text-slate-300 transition-all duration-300 hover:bg-white/5 hover:text-white"
            style={`--delay: ${index * 50}ms`}
          >
            <span class="relative z-10 flex items-center gap-3">
              <span class="h-1.5 w-1.5 rounded-full bg-cyan-500 opacity-0 transition-opacity duration-300 group-hover:opacity-100" />
              {item.label}
            </span>
            <span class="absolute top-0 left-0 h-full w-1 rounded-full bg-gradient-to-b from-cyan-500 to-violet-500 opacity-0 transition-opacity duration-300 group-hover:opacity-100" />
          </a>
        ))
      }
    </nav>

    <!-- MOBILE CTA BUTTON -->
    <div class="mt-auto border-t border-white/10 pt-6">
      <a
        href="#contacto"
        class="mobile-menu-link btn-cta btn-cta--solid w-full justify-center py-4 text-lg"
        style="--delay: 200ms"
      >
        Contacto
        <span class="btn-arrows" aria-hidden="true">
          <svg viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M3 8h10M9 4l4 4-4 4"></path></svg>
          <svg viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M3 8h10M9 4l4 4-4 4"></path></svg>
        </span>
      </a>
    </div>

    <!-- MOBILE MENU FOOTER -->
    <div class="mt-6 text-center text-sm text-slate-500">
      <p>© 2026 Yzen</p>
    </div>
  </div>
</div>

<style>
  /* Pill camaleón: las vars --nav-* las setea motion.ts via [data-nav-theme]
     en :root; el CSS de global.css define los valores por tema. */
  .nav-pill {
    background-color: rgb(255 255 255 / 0.94);
    border-color: rgb(15 23 42 / 0.1);
    color: #0f172a;
    -webkit-backdrop-filter: blur(18px);
    backdrop-filter: blur(18px);
    transition:
      color 300ms var(--ease-out-expo),
      background-color 300ms var(--ease-out-expo),
      border-color 300ms var(--ease-out-expo);
  }

  .nav-link {
    color: #0f172a;
    transition: color 300ms var(--ease-out-expo), opacity 200ms ease;
  }

  .nav-link:hover {
    opacity: 0.85;
  }

  .mobile-nav-cta,
  .mobile-nav-menu {
    height: 3.25rem;
    border-radius: 0.65rem;
    background: #f1f5f9;
    color: #0f172a;
  }

  .mobile-nav-cta {
    display: inline-flex;
    min-width: 8.4rem;
    align-items: center;
    justify-content: center;
    padding-inline: 1.15rem;
    font-size: 0.82rem;
    font-weight: 600;
    background: #ecfeff;
    color: #052e3e;
  }

  .mobile-nav-menu {
    width: 3.25rem;
  }

  .nav-contact {
    border-color: #052e3e;
    background-color: #052e3e;
    color: #ecfeff;
    box-shadow: 0 0.35rem 1rem rgb(5 46 62 / 0.16);
  }

  .nav-contact:hover {
    border-color: #164e63;
    background-color: #164e63;
    color: #ffffff;
    box-shadow: 0 0.5rem 1.25rem rgb(5 46 62 / 0.24);
  }

  .nav-contact:focus-visible {
    outline-color: rgb(8 145 178 / 0.55);
  }

  .nav-contact .btn-arrows {
    position: relative;
    display: inline-block;
    width: 1em;
    height: 1em;
    overflow: hidden;
    flex: none;
  }

  .nav-contact .btn-arrows > svg {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    transition: transform 300ms var(--ease-out-expo);
  }

  .nav-contact .btn-arrows > svg:nth-child(2) {
    transform: translateX(-150%);
  }

  .nav-contact:hover .btn-arrows > svg:first-child {
    transform: translateX(150%);
  }

  .nav-contact:hover .btn-arrows > svg:nth-child(2) {
    transform: translateX(0);
  }

  /* Swap de logos por tema, solo CSS (sin JS). Default: tema dark → blanco. */
  .nav-logo-color,
  .nav-logo-white {
    transition: opacity 300ms var(--ease-out-expo);
  }

  .nav-logo-color {
    opacity: 1;
  }

  .nav-logo-white {
    opacity: 0;
  }

  #site-nav[data-nav-theme="light"] .nav-logo-color {
    opacity: 1;
  }

  #site-nav[data-nav-theme="light"] .nav-logo-white {
    opacity: 0;
  }

  .mobile-menu-link {
    opacity: 0;
    transform: translateX(20px);
    transition:
      opacity 0.3s ease,
      transform 0.3s ease,
      background-color 0.3s ease,
      color 0.3s ease;
    transition-delay: var(--delay, 0ms);
  }

  #mobileMenuPanel.open .mobile-menu-link {
    opacity: 1;
    transform: translateX(0);
  }
</style>

<script>
  const menuToggle = document.getElementById("menuToggle");
  const mobileMenuOverlay = document.getElementById("mobileMenuOverlay");
  const mobileMenuPanel = document.getElementById("mobileMenuPanel");
  const menuIconOpen = menuToggle?.querySelector(".menu-icon-open");
  const menuIconClose = menuToggle?.querySelector(".menu-icon-close");
  const mobileMenuLinks = document.querySelectorAll(".mobile-menu-link");

  let isMenuOpen = false;

  function openMenu(): void {
    isMenuOpen = true;
    mobileMenuOverlay?.classList.remove("opacity-0", "pointer-events-none");
    mobileMenuOverlay?.classList.add("opacity-100", "pointer-events-auto");
    mobileMenuPanel?.classList.remove("translate-x-full");
    mobileMenuPanel?.classList.add("translate-x-0", "open");
    menuIconOpen?.classList.add("hidden");
    menuIconClose?.classList.remove("hidden");
    menuToggle?.setAttribute("aria-expanded", "true");
    menuToggle?.setAttribute("aria-label", "Cerrar menú");
    document.body.style.overflow = "hidden";
  }

  function closeMenu(): void {
    isMenuOpen = false;
    mobileMenuOverlay?.classList.add("opacity-0", "pointer-events-none");
    mobileMenuOverlay?.classList.remove("opacity-100", "pointer-events-auto");
    mobileMenuPanel?.classList.add("translate-x-full");
    mobileMenuPanel?.classList.remove("translate-x-0", "open");
    menuIconOpen?.classList.remove("hidden");
    menuIconClose?.classList.add("hidden");
    menuToggle?.setAttribute("aria-expanded", "false");
    menuToggle?.setAttribute("aria-label", "Abrir menú");
    document.body.style.overflow = "";
  }

  function toggleMenu(): void {
    if (isMenuOpen) {
      closeMenu();
    } else {
      openMenu();
    }
  }

  menuToggle?.addEventListener("click", toggleMenu);
  mobileMenuOverlay?.addEventListener("click", closeMenu);

  mobileMenuLinks.forEach((link) => {
    link.addEventListener("click", closeMenu);
  });

  document.addEventListener("keydown", (e) => {
    if (e.key === "Escape" && isMenuOpen) {
      closeMenu();
    }
  });

  window.addEventListener("resize", () => {
    if (window.innerWidth >= 768 && isMenuOpen) {
      closeMenu();
    }
  });
</script>
