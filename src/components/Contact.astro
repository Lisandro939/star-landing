---
import { Icon } from 'astro-icon/components';
---

<section class="py-24 bg-slate-950" id="contacto">
  <div class="container mx-auto px-6">
    <div class="max-w-6xl mx-auto rounded-[3rem] bg-white/[0.02] border border-white/10 overflow-hidden shadow-2xl">
      <div class="grid lg:grid-cols-2">
        <div class="p-12 md:p-20 bg-gradient-to-br from-cyan-500/10 to-indigo-500/10">
          <h2 class="text-4xl md:text-6xl font-normal text-white mb-8 font-display leading-[1.1]">
            ¿Listo para dar el <span class="text-gradient">gran salto</span>?
          </h2>
          <p class="text-slate-400 text-lg mb-12">
            Estamos buscando el próximo gran desafío. Cuéntanos sobre tu visión y nosotros pondremos la ingeniería.
          </p>

          <div class="space-y-8">
            <div class="flex items-center gap-6">
              <div class="w-14 h-14 rounded-2xl bg-white/5 border border-white/10 flex items-center justify-center text-cyan-400">
                <Icon name="lucide:mail" size={24} />
              </div>
              <div>
                <p class="text-xs font-bold text-slate-500 uppercase tracking-widest mb-1">Email</p>
                <p class="text-white font-bold">solutions@star-soft.io</p>
              </div>
            </div>
            
            <div class="flex items-center gap-6">
              <div class="w-14 h-14 rounded-2xl bg-white/5 border border-white/10 flex items-center justify-center text-indigo-400">
                <Icon name="lucide:message-square" size={24} />
              </div>
              <div>
                <p class="text-xs font-bold text-slate-500 uppercase tracking-widest mb-1">Feedback</p>
                <p class="text-white font-bold">Disponibilidad 24/7</p>
              </div>
            </div>
          </div>
        </div>

        <form class="p-12 md:p-20 bg-slate-950/40" id="contactForm">
          <div class="space-y-6">
              <div class="space-y-2">
                <label class="text-xs font-bold text-slate-500 uppercase tracking-widest ml-1">Nombre</label>
                <input type="text" class="w-full px-6 py-4 rounded-2xl bg-white/5 border border-white/10 text-white focus:outline-none focus:border-cyan-400 transition-colors" placeholder="Alex" required />
              </div>
              <div class="space-y-2">
                <label class="text-xs font-bold text-slate-500 uppercase tracking-widest ml-1">Compañía</label>
                <input type="text" class="w-full px-6 py-4 rounded-2xl bg-white/5 border border-white/10 text-white focus:outline-none focus:border-cyan-400 transition-colors" placeholder="Astro Corp" />
              </div>
            <div class="space-y-2">
              <label class="text-xs font-bold text-slate-500 uppercase tracking-widest ml-1">Email</label>
              <input type="email" class="w-full px-6 py-4 rounded-2xl bg-white/5 border border-white/10 text-white focus:outline-none focus:border-cyan-400 transition-colors" placeholder="alex@astro.build" required />
            </div>
            <div class="space-y-2">
              <label class="text-xs font-bold text-slate-500 uppercase tracking-widest ml-1">Mensaje</label>
              <textarea rows="4" class="w-full px-6 py-4 rounded-2xl bg-white/5 border border-white/10 text-white focus:outline-none focus:border-cyan-400 transition-colors resize-none" placeholder="¿Qué estás construyendo?" required></textarea>
            </div>
            <button type="submit" class="w-full py-5 rounded-2xl bg-white text-slate-950 font-black text-lg hover:bg-cyan-400 transition-all active:scale-95 flex items-center justify-center gap-3">
              Enviar Propuesta
              <Icon name="lucide:arrow-right" size={20} />
            </button>
          </div>
        </form>
      </div>
    </div>
  </div>
</section>

<script>
  const form = document.getElementById('contactForm') as HTMLFormElement;
  form?.addEventListener('submit', (e) => {
    e.preventDefault();
    alert('¡Propuesta recibida! Nos pondremos en contacto pronto.');
    form.reset();
  });
</script>
